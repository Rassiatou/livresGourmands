# Développement local : API Node sur le port 3001
VITE_API_URL=http://localhost:3001/api

# Sur Vercel (production) : le front utilise /api sur le même domaine + la fonction api/proxy.js.
# Définir l’URL du serveur backend (sans /api), ex. https://livres-api.railway.app
# LIVRES_BACKEND_ORIGIN=https://votre-backend.example.com
